services:
  tests:
    image: composer:2
    working_dir: /app
    volumes:
      - ./:/app
      - composer-cache:/tmp/cache
    environment:
      COMPOSER_CACHE_DIR: /tmp/cache
    command: sh -lc "composer install --no-interaction --prefer-dist && vendor/bin/phpunit"

volumes:
  composer-cache:
